# 2026-05-13 系统维护日报

## 心跳触发
- **触发时间**: 2026-05-13 21:18 (Asia/Shanghai)
- **巡检窗口**: 过去 24h (05-13 00:00 ~ 21:18)

## 日志概览 (2026-05-13)
- **WARN**: ~1121 条（plugins 451 + gateway 187 + skills 147 + agent/embedded 128 + 其他~208）
- **ERROR**: ~327 条（feishu 12 + tools read 22 + gateway 10 + LLM 4 + 其他~279，含大量重复的 preload WARN 被错误标记）
- **FATAL**: 0 条

## 新增问题

### 1. 📋 ai-news-research 持续 delivery 失败（4次）
- **上次状态**: ok（05-13 10:38 修复 mode=silent→announce 后）
- **当前状态**: ❌ 连续 4 次失败（consecutiveErrors=4）
- **错误**: `"Delivering to Feishu requires target <chatId|user:openId|chat:chatId>"`
- **现状**: delivery 配置 `{channel: "feishu", to: "user:ou_bd0bb7c1d6a89633ec6a14ec8c04ef61"}` 与 ai-daily-news 完全一致，但 ai-news-research 持续失败
- **可能原因**: `to` 字段可能未被 Feishu channel 识别为 `target`；或 Job 的 state 状态锁在 error 状态
- **建议操作**: 在 delivery 配置中同时加入 `"target": "user:ou_bd0bb7c1d6a89633ec6a14ec8c04ef61"` 字段

### 2. 🔄 test job b6b81114-15e0-4fb0-938d-9fffe443dea6 已超时
- agentId: lifehelper, schedule: at 2026-05-13T02:00:00.000Z
- 超时 120s，状态 unknown
- **建议操作**: 清理此测试任务

### 3. ⚠️ LLM timeout + fallback 401 (4次)
- deepseek-v4-pro 超时后 fallback 到 deepseek-v4-flash 返回 HTTP 401
- 401 属于预期行为（无 deepseek 直连 API key）
- **状态**: 监控即可

### 4. 🟡 Skills 路径警告 (147x)
- `"Skipping skill path that resolves outside its configured root"` - huashu-nuwa symlink
- **状态**: 已知良性

## 配置变更
| 时间 | 变更 | 原因 |
|------|------|------|
| 2026-05-13 21:07 | `blockStreamingDefault`: "off" → "on" | 用户反馈 Feishu 流式输出不生效（感觉不到逐段刷新） |

## Cron 任务状态
- reset 后所有 job 的 `lastRunStatus` 均为 null（运行时状态被清除）
- 实际执行记录仍保留在 `cron/runs/*.jsonl`
- 下一轮执行后状态将恢复

### 5. 🔍 ai-news-research vs ai-daily-news delivery 配置对比 ⭐
- **时间**: 2026-05-13 21:39 排查
- **发现**: 两个 Job 的 `delivery` 配置**一字不差**：
  ```
  {mode: "announce", channel: "feishu", to: "user:ou_bd0bb7c1d6a89633ec6a14ec8c04ef61"}
  ```
- **结果差异**: ai-daily-news → delivered ✅ | ai-news-research → "requires target" ❌（连续4次）
- **排除因素**: 配置相同、target 相同、均手动触发（19:30/19:35）
- **疑点**: Cron runner 对 `to` 字段的处理可能存在 session/agent 相关的不确定性
- **待验证**: 添加 `target` 字段后是否解决

### 6. 🟡 guidor deepseek-v4-pro 晚间持续超时（21:17~21:38）
- **时间**: 2026-05-13 21:17~21:38 排查用户"回复为啥会这么慢"
- **现象**: guidor 网关 deepseek-v4-pro 一小时内连续超时：
  - 21:17 — 600s timeout → lane task error（sysadmin session / main lane）
  - 21:26 — 180s timeout → memory-persona-generation 同样超时
  - 21:18 / 21:25 / 21:38 — compaction retry aggregate timeout ×3（各 60s）
- **Fallback 链断裂**: 超时后 fallback 到 deepseek-v4-flash 直连 → HTTP 401（无 deepseek 直连 API key），超时等于白等
- **Session 状态**: 当前 session 39k token / 34 次 compaction，上下文膨胀加剧延迟
- **根因**: guidor 后端或 deepseek 服务端晚间波动，非本地问题
- **建议**: 若持续，可临时切模型（如 guidor 上其他可用模型）

### 7. 🔴 Feishu 流式输出不工作 — 根因定位（22:00~22:08）
- **问题**: 用户反馈改完 `blockStreamingDefault` 后流式输出仍不生效
- **诊断**: 直接调用 guidor/deepseek-v4-pro API 测试

| 条件 | streaming chunks 的 `content` 字段 | `reasoning_content` 字段 | blockStreaming 效果 |
|------|------------------------------------|--------------------------|---------------------|
| 不传 `thinking` | 全部 `null` | 有文字 | ❌ 不触发块推送 |
| 传 `thinking: {type: "disabled"}` | 有文字 | 无 | ✅ 正常推送 |

- **根因链**: DeepSeek V4 默认启用 thinking → 文本进 `reasoning_content` → OpenClaw blockStreaming 只监听 `content` → 永远凑不够 800 字符块 → 一次性全量输出
- **影响范围**: guidor deepseek-v4-pro、guidor deepseek-v4-flash、直接 deepseek API — 全部相同行为
- **修复**: 在 `agents.defaults.models` 的 `guidor/deepseek-v4-pro` 条目添加 `"params": {"thinking": "off"}`（22:08）
- **验证待定**: 不确定 OpenClaw 是否将 `params.thinking` 透传到 API 调用；需用户测试确认

### 8. 🔴 thinking 双层配置导致流式感知延迟（22:14~22:17） ⭐
- **触发**: 用户反馈"没感觉到"流式提速效果，尽管模型层 `params.thinking: "off"` 已设置
- **诊断**: 检查 `agents.defaults` 发现 `thinkingDefault: "low"` — OpenClaw 层仍在进行内部推理，推理内容不输出但耗时可感知
- **根因**: thinking 有两层独立控制：
  1. 模型层 `params.thinking: "off"` — 控制 API 调用的 thinking 参数（关掉 `reasoning_content`）
  2. OpenClaw 层 `thinkingDefault` — 控制 Agent 是否在生成前做内部推理
  两者独立，仅关模型层不够
- **修复**: `thinkingDefault`: "low" → "off"（22:17）
- **验证待定**: 用户测试确认速度改善

## 建议与跟进
1. **⌛ 流式输出**: `thinkingDefault: "off"` + `params.thinking: "off"` 双层关闭，用户测试确认
2. **ai-news-research**: 在 delivery 配置中同时加入 `"target"` 字段（与 `to` 并存）
3. **test job**: 建议清理超时测试任务
4. **监控**: LLM timeout 晚间时段明显增多（3次/小时），持续观察；若频发考虑切换模型网关

### 9. 🔴 DeepSeek V4 thinking 禁用方案修正（22:49）⭐⭐⭐
- **触发**: 用户要求搜索外部方案而非闭门造车
- **搜索发现**:
  - OpenClaw GitHub #71050: DeepSeek V4 thinking mode 多轮对话 400 错误，官方建议禁用
  - OpenClaw GitHub #74374: thinking=disabled 时 DeepSeek 仍偶尔返回 reasoning_content 导致 400
  - DeepSeek API 文档: thinking 禁用必须用 `{"thinking": {"type": "disabled"}}` 格式
- **关键发现**: `params.thinking: "off"` 在 OpenClaw 中映射的是 **fast-mode**（优先处理开关），不是 DeepSeek 的思考模式！
  - 这是两套完全独立的系统：OpenClaw `/think` vs DeepSeek 原生 thinking
- **修复**: guidor/deepseek-v4-pro 模型配置添加 `"extra_body": {"thinking": {"type": "disabled"}}`（22:49）
  - `extra_body` 会透传到 OpenAI-compatible API 调用的额外字段，deepseek API 读取此字段
  - `thinkingDefault: "off"`（OpenClaw 层）+ `extra_body`（API 层）= 双层关闭
- **教训**:
  - ✅ OpenClaw 的 `params.thinking` ≠ DeepSeek 的 thinking — 命名冲突导致混淆
  - ✅ `extra_body` 是向 API 透传参数的正确方式
  - ✅ 遇到问题先搜索 GitHub Issues + 官方文档，不做猜测试错
  - ⚠️ guidor 代理网关是否透传 extra_body 待验证
- **验证待定**: 用户测试确认速度改善

### 12. 🔄 临时方案：切回 Flash（23:16~23:19）
- **操作**: `agents.defaults.model` 恢复为 `guidor/deepseek-v4-flash`
  - Flash 不启用 thinking 模式，无 reasoning_content 问题
  - 速度显著改善
- **代价**: 模型能力从 Pro 降级到 Flash
- **Gateway 重启**: 尝试 `openclaw gateway restart` 被用户中断 → 手动确认后完成
  - 重启后状态正常（pid 3938291, RPC ok）
- **Pro 持久修复路径**: 
  1. guidor 服务端添加 `thinking: disabled` 端点配置
  2. 或本地部署透明代理在请求上注入 `extra_body: {thinking: {type: "disabled"}}`
  3. 或 OpenClaw 未来版本支持 `agents.defaults.models` 下放 `extra_body`
