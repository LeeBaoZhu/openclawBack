# 2026-04-02 系统维护记录

## 定时任务执行 (00:00 巡检)

### 日志概览 (过去 24h)
- **Warning**: ~15 条 (安全配置警告、插件信任警告、memory-tdai 降级)
- **Error**: ~50 条 (Cron 投递失败、Feishu 卡片创建失败、Anthropic API 密钥缺失)
- **Fatal**: 0 条

### 配置变更
- openclaw.json: 无变更 (最后更新 2026-03-27)
- cron/jobs.json: 无变更

---

## ❗ 严重问题：所有 Cron 任务投递失败

### 问题描述
全部 5 个 Cron 任务 `lastRunStatus: "error"`，通知投递失败导致任务无法完成。

### 受影响任务
| 任务 ID | 渠道 | 错误信息 |
|--------|------|----------|
| sysadmin-log-rotation | feishu | `Delivering to Feishu requires target <chatId\|user:openId\|chat:chatId>` |
| daily-plan | feishu | `Delivering to Feishu requires target <chatId\|user:openId\|chat:chatId>` |
| sysadmin-daily-log-check | feishu | (无具体错误，但状态为 error) |
| sysadmin-daily-git-push | feishu | `Delivering to Feishu requires target <chatId\|user:openId\|chat:chatId>` |
| lifehelper-daily-review-reminder | lightclawbot | `No delivery target for lightclawbot. Specify a target with --to or configure allowFrom.` |

### 可能原因
1. **Feishu 渠道**：`delivery.target` 格式虽然为 `"user:ou_bd0bb7c1d6a89633ec6a14ec8c04ef61"`，但可能：
   - 该 open_id 对应的用户已禁用/退出应用
   - 飞书应用权限变更导致无法发送消息
   - 需要检查飞书应用后台的用户授权状态

2. **lightclawbot 渠道**：缺少 `allowFrom` 配置或未指定 `--to` 目标
   - 需要在 openclaw.json 中配置 `channels.lightclawbot.allowFrom` 或在 cron 任务中指定 `delivery.to`

### 建议操作
1. **立即**：检查飞书开放平台后台，确认 `ou_bd0bb7c1d6a89633ec6a14ec8c04ef61` 用户是否仍授权给 sysadmin 应用
2. **立即**：测试通过 feishu 渠道手动发送消息，验证渠道是否可用
3. **修复 lightclawbot**：在 cron 任务中添加 `delivery.to: "lightclawbot:<user_id>"` 或配置 `channels.lightclawbot.allowFrom`
4. **临时方案**：如飞书渠道无法恢复，可暂时将 `delivery.channel` 改为 `lightclawbot` 并配置正确的 target

### 解决方案
```json
// 方案 1: 修复 lightclawbot 渠道配置 (openclaw.json)
{
  "channels": {
    "lightclawbot": {
      "enabled": true,
      "apiKeys": [...],
      "allowFrom": ["*"]  // 或指定具体用户
    }
  }
}

// 方案 2: 修改 cron 任务 delivery.to (cron/jobs.json)
{
  "delivery": {
    "mode": "announce",
    "channel": "lightclawbot",
    "to": "lightclawbot:<user_openid>"
  }
}
```

---

## 其他问题

### 1. memory-tdai LLM 提取失败 ℹ️
- **错误**: `No API key found for provider "anthropic"`
- **影响**: L1 记忆向量提取不可用，回退到关键词搜索
- **建议**: 如需要向量搜索，配置 anthropic API 密钥或安装 sqlite-vec 模块
- **状态**: 可选功能，不影响核心使用

### 2. Feishu 卡片创建失败 (偶发) ℹ️
- **错误**: `Create card request failed with HTTP 400`
- **影响**: 流式响应创建失败，但不影响普通消息发送
- **建议**: 持续观察，如高频出现需检查飞书应用配置
- **状态**: 偶发，非阻塞性

### 3. 日志文件路径问题 ℹ️
- **配置**: `logging.file: "/tmp/openclaw.log"`
- **现象**: 工具访问 `/root/.openclaw/openclaw.log` 报 ENOENT
- **影响**: 不影响实际日志记录（Gateway 进程管理）
- **建议**: 通过 `openclaw logs --follow` 或 Control UI 查看日志

---

## 本次巡检总结
**系统存在严重问题：所有 Cron 任务因投递失败而报错**。需立即修复通知渠道配置，否则每日定时任务（日志巡检、计划推送、Git 同步、晚间回顾）均无法完成通知。

**优先级**: 🔴 高 - 需在 24h 内修复

---

## ✅ 定时任务执行记录

### sysadmin-daily-git-push (01:00 执行)
- **状态**: 成功
- **提交**: `843d1a3e` - daily-auto: 2026-04-02
- **变更**: 29 files changed, 2575 insertions(+), 297 deletions(-)
- **推送**: origin/main 成功

### sysadmin-log-rotation (02:00 执行)
- **状态**: ✅ 成功
- **执行时间**: 2026-04-02 02:00:05 CST
- **日志大小**: 31MB (正常，无需轮转)
- **清理操作**: 已清理 7 天前过期备份
- **备份目录大小**: 1.4M
- **剩余备份文件数**: 1

---

_自动记录 by SysAdmin 🔧_
