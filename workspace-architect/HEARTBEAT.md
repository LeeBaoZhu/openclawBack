# HEARTBEAT.md - 心跳巡检（Architect）

本 Agent 为 **7d** 心跳（见 `openclaw.json`）。轮询时先读 **`AGENTS.md`** 与 **`HEARTBEAT.md`**。

- **无正文清单**：可按默认提示检查技术进展、与 ProductOwner 同步；无事项回复 `HEARTBEAT_OK`。
- **有清单**：严格按下列条目执行。

---

## 建议巡检（可删改）

- 本周技术进展与风险是否有需同步给 ProductOwner 的事项
- `docs/` 与团队区 `workspace-product-team/` 是否需要更新

（无则删除上表，仅保留文件头说明亦可。）
